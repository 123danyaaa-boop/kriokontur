# Проверенное окружение: Python 3.12.2 (минимум 3.10), Windows 11 и Linux.
# Версии зафиксированы, чтобы прогон жюри повторял наши числа бит-в-бит.
# Диапазон совместимости, на котором тесты тоже проходят, указан в комментарии.
pyyaml==6.0.3            # >=6.0
openpyxl==3.1.5          # >=3.1
fastapi==0.141.1         # >=0.110
uvicorn==0.53.0          # >=0.29
pydantic==2.13.5         # >=2.6
pytest==9.1.1            # >=8.0
pulp==3.3.2              # >=2.7, решатель CBC входит в пакет
