version: "3.9"

services:

  backend:
    build: .
    environment:
      # явно: внутри контейнера сервер слушает все интерфейсы, иначе порты снаружи не откроются
      - KRIOKONTUR_HOST=0.0.0.0
    ports:
      - "8000-8010:8000-8010"